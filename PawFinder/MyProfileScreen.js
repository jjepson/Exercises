import React from 'react';
import {Text, ScrollView} from 'react-native';

export class MyProfileScreen extends React.Component {
  render() {
    return (
      <ScrollView><Text>Pet Profile</Text></ScrollView>
    );
  }
}